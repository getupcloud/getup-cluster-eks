{{- if not (or .Values.kustomize .Values.manifests .Values.apps .Values.keys) }}
{{- fail "You must enable one of: kustomize=true, manifests=true, apps=true, valuesKeys=true" }}
{{- end }}

{{- if and .Values.kustomize .Values.manifests }}
{{- fail "You must enable only one of kustomize=true or manifests=true" }}
{{- end }}
