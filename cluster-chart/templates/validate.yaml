{{- if not (or .Values.kustomize .Values.manifests) }}
{{- fail "You must enable either kustomize=true or manifests=true" }}
{{- end }}

{{- if and .Values.kustomize .Values.manifests }}
{{- fail "You must enable only one of kustomize=true or manifests=true" }}
{{- end }}